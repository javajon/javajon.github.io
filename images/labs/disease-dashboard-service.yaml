apiVersion: v1
kind: Service
metadata:
  name: disease-dashboard
  labels:
    app: disease-dashboard
spec:
  type: NodePort
  selector:
    app: disease-dashboard
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001
