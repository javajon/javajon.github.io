apiVersion: v1
kind: Service
metadata:
  name: assembler-service
  namespace: message-app
spec:
  selector:
    app: assembler
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30011
  type: NodePort