apiVersion: v1
kind: Service
metadata:
  name: climate-dashboard
  namespace: dashboard-system
spec:
  type: NodePort
  selector:
    app: climate-dashboard
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30001
