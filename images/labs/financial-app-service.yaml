apiVersion: v1
kind: Service
metadata:
  name: financial-app
  labels:
    app: financial-app
spec:
  type: ClusterIP
  selector:
    app: financial-app
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http